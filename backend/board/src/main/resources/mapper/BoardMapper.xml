<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.BoardMapper">

    <!-- 예시: 게시글 등록 -->
    <insert id="save" parameterType="com.example.demo.board.dto.BoardDTO">
        INSERT INTO board_table (
            boardTitle, boardContents, boardHits, createdAt, fileAttached, memberId
        ) VALUES (
            #{boardTitle}, #{boardContents}, #{boardHits}, NOW(), #{fileAttached}, #{memberId}
        );
    </insert>

    <!-- 예시: 게시글 목록 조회 -->
    <select id="findAll" resultType="com.example.demo.board.dto.BoardDTO">
        SELECT * FROM board_table ORDER BY id DESC;
    </select>

    <select id="findById" parameterType="Long" resultType="com.example.demo.board.dto.BoardDTO">
        SELECT * FROM board_table WHERE id = #{id};
    </select>

</mapper>